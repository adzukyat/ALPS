/*
 * Design tokens and component styles for the Maneuver For VRC clip inspector.
 *
 * Every size here is border-box, as UI Toolkit always is: a value box's 70px includes
 * its padding and border.
 *
 * The styles are dark-only. The light editor skin is not a target.
 */

.mfv-root {
    --mfv-card-bg: #333333;
    --mfv-panel-bg: #3f3f3f;
    --mfv-header-bg: #585858;
    --mfv-sub-bg: #373737;
    --mfv-nested-bg: #303030;
    --mfv-well-bg: #1f1f1f;
    --mfv-control-bg: #242424;
    --mfv-field-bg: #232323;
    --mfv-tile-bg: #2a2a2a;

    --mfv-line: #202020;
    --mfv-line-strong: #141414;
    --mfv-line-soft: #262626;
    --mfv-line-tile: #171717;
    --mfv-track: #282828;
    --mfv-tick: #6e6e6e;

    --mfv-accent: #2a78d6;
    --mfv-accent-line: #1d5fae;
    --mfv-accent-soft: #6ea8dc;
    --mfv-accent-tile: #1f4d80;
    --mfv-art: #d9a441;

    --mfv-text: #e4e4e4;
    --mfv-text-strong: #f2f2f2;
    --mfv-text-value: #f4f4f4;
    --mfv-text-label: #eaeaea;
    --mfv-text-sub: #dcdcdc;
    --mfv-text-muted: #bdbdbd;
    --mfv-text-desc: #c6c6c6;
    --mfv-text-hint: #a8a8a8;
    --mfv-danger: #e8a5a5;

    --mfv-label-width: 96px;
    --mfv-row-gap: 9px;

    font-size: 12.5px;
    color: var(--mfv-text);
    /* Transparent so the native inspector background shows through and the view reads as
       part of the IMGUI clip inspector above it. */
    background-color: rgba(0, 0, 0, 0);
    padding: 11px 12px 14px 12px;
    /* The inspector is laid out for a 476px card. Below that the host scrolls rather than
       letting segmented controls overrun their row. */
    min-width: 476px;
}

/* ------------------------------------------------------------ vertical flow
 *
 * UI Toolkit 2022.3 has no `gap`, so every flow container states the spacing its
 * children carry and compensates with a reduced padding-bottom (the last child's
 * margin is not trimmed). Rules are keyed on the container so a nested panel
 * inside a sub-panel takes the *sub*-panel's spacing, not its own.
 */

.mfv-root > * {
    margin-bottom: 2px;
    flex-shrink: 0;
}

.mfv-root .mfv-stack > * {
    margin-bottom: 2px;
    flex-shrink: 0;
}

.mfv-root .mfv-flow9 > * {
    margin-bottom: 9px;
    flex-shrink: 0;
}

.mfv-root .mfv-sub > * {
    margin-bottom: 7px;
    flex-shrink: 0;
}

.mfv-root .mfv-nested > * {
    margin-bottom: 3px;
    flex-shrink: 0;
}

/* ---------------------------------------------------------------- rows */

.mfv-row {
    flex-direction: row;
    align-items: center;
}

/* Out-specifies `.mfv-root .unity-base-field { align-items: center }`, which would
   otherwise centre the easing / palette label against a two-row grid. */
.mfv-root .unity-base-field.mfv-row--top,
.mfv-root .mfv-row.mfv-row--top {
    align-items: flex-start;
}

.mfv-root .mfv-row--top .unity-base-field__label,
.mfv-root .mfv-row--top .mfv-row__label {
    /* Nudges the label onto the first tile's baseline. */
    padding-top: 3px;
}

.mfv-row__label {
    width: var(--mfv-label-width);
    flex-shrink: 0;
    color: var(--mfv-text-label);
    -unity-font-style: bold;
    margin-right: var(--mfv-row-gap);
}

.mfv-row--sub .mfv-row__label {
    color: var(--mfv-text-sub);
    -unity-font-style: normal;
}

.mfv-row__hint {
    font-size: 11px;
    color: var(--mfv-text-hint);
    margin-left: var(--mfv-row-gap);
}

.mfv-divider {
    height: 2px;
    background-color: #2b2b2b;
    margin: 16px 8px 0 8px;
}

/*
 * Zero the editor's own field margins, but never margin-bottom: that belongs to the
 * flow rules above and a tie on specificity would make the spacing order-dependent.
 */
.mfv-root .unity-base-field {
    margin-top: 0;
    margin-left: 0;
    margin-right: 0;
    flex-direction: row;
    align-items: center;
    /* Without this a field cannot shrink below its content and overflows the row. */
    min-width: 0;
}

.mfv-root .unity-base-field__label {
    width: var(--mfv-label-width);
    min-width: var(--mfv-label-width);
    flex-shrink: 0;
    color: var(--mfv-text-label);
    -unity-font-style: bold;
    margin: 0 var(--mfv-row-gap) 0 0;
    padding: 0;
}

.mfv-root .unity-base-field__input {
    /* flex-basis: 0 makes the input size from the space left after the fixed-width
       label, rather than from its own content width. */
    flex-basis: 0;
    flex-grow: 1;
    min-width: 0;
    flex-direction: row;
    align-items: center;
}

/* --------------------------------------------------- segmented control */

.mfv-root .mfv-seg__input {
    flex-grow: 0;
    flex-basis: auto;
    flex-direction: row;
    background-color: var(--mfv-control-bg);
    border-width: 1px;
    border-color: var(--mfv-line-strong);
    border-radius: 4px;
    overflow: hidden;
    align-self: flex-start;
}

.mfv-seg__item {
    padding: 4px 12px 4px 12px;
    color: var(--mfv-text-muted);
    background-color: rgba(0, 0, 0, 0);
    border-left-width: 1px;
    border-left-color: #212121;
    -unity-text-align: middle-center;
}

.mfv-seg__item--first {
    border-left-width: 0;
}

.mfv-seg__item--selected {
    background-color: var(--mfv-accent);
    color: #ffffff;
}

.mfv-seg--compact .mfv-seg__item {
    padding: 4px 11px 4px 11px;
}

/* -------------------------------------------------------- tab selector */

.mfv-root .mfv-tabs__input {
    flex-direction: row;
    background-color: var(--mfv-control-bg);
    border-width: 1px;
    border-color: var(--mfv-line-strong);
    border-radius: 5px;
    padding: 3px;
}

.mfv-tabs .unity-base-field__label {
    display: none;
}

.mfv-tabs__item {
    flex-grow: 1;
    flex-basis: 0;
    min-width: 0;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    padding: 8px 0 8px 0;
    border-radius: 3px;
    color: var(--mfv-text-muted);
    margin-right: 3px;
}

.mfv-tabs__item--last {
    margin-right: 0;
}

.mfv-tabs__item--selected {
    background-color: var(--mfv-accent);
    color: #ffffff;
    -unity-font-style: bold;
}

.mfv-tabs__icon {
    width: 17px;
    height: 17px;
    margin-right: 7px;
}

/* -------------------------------------------------------- toggle switch */

.mfv-root .mfv-switch__input {
    flex-grow: 0;
    flex-basis: auto;
    align-self: flex-start;
}

.mfv-switch__track {
    width: 36px;
    height: 20px;
    border-radius: 10px;
    background-color: #4d4d4d;
    border-width: 1px;
    border-color: var(--mfv-line-soft);
}

.mfv-switch__track--on {
    background-color: var(--mfv-accent);
    border-color: var(--mfv-accent-line);
}

.mfv-switch__knob {
    position: absolute;
    top: 3px;
    left: 3px;
    width: 12px;
    height: 12px;
    border-radius: 6px;
    background-color: #e6e6e6;
}

.mfv-switch__knob--on {
    left: 19px;
    background-color: #ffffff;
}

/* ------------------------------------------------------- range "R" flag */

.mfv-root .mfv-rangeflag {
    flex-shrink: 0;
    flex-grow: 0;
    margin-left: var(--mfv-row-gap);
}

.mfv-root .mfv-rangeflag.mfv-spreadflag {
    margin-left: 3px;
}

.mfv-root .mfv-rangeflag__input {
    flex-grow: 0;
    flex-basis: auto;
}

.mfv-rangeflag__box {
    width: 24px;
    height: 21px;
    border-width: 1px;
    border-color: #6d6d6d;
    border-radius: 3px;
    color: #c2c2c2;
    font-size: 10.5px;
    -unity-font-style: bold;
    -unity-text-align: middle-center;
    padding: 0;
}

.mfv-rangeflag__box--on {
    border-color: var(--mfv-accent);
    background-color: var(--mfv-accent);
    color: #ffffff;
}

/* --------------------------------------------------------------- fields */

.mfv-root .mfv-numberbox__input {
    flex-grow: 0;
    flex-shrink: 0;
    flex-basis: auto;
    width: 70px;
}

.mfv-numberbox {
    width: 70px;
    flex-shrink: 0;
    background-color: var(--mfv-field-bg);
    border-width: 1px;
    border-color: var(--mfv-line-strong);
    border-radius: 3px;
    padding: 0;
    color: var(--mfv-text-value);
    -unity-text-align: middle-right;
}

/*
 * MfvNumberBox is itself a BaseField, so these have to out-specify
 * `.mfv-root .unity-base-field { margin-left/right: 0 }`. Unscoped they lost, and the
 * value box sat flush against the track with the min thumb drawn inside it.
 */
.mfv-root .mfv-numberbox--left {
    margin-right: var(--mfv-row-gap);
}

.mfv-root .mfv-numberbox--right {
    margin-left: var(--mfv-row-gap);
}

.mfv-textbox {
    flex-basis: 0;
    flex-grow: 1;
    min-width: 0;
    background-color: var(--mfv-field-bg);
    border-width: 1px;
    border-color: var(--mfv-line-strong);
    border-radius: 3px;
    color: var(--mfv-text-value);
}

.mfv-textbox .unity-base-text-field__input {
    background-color: rgba(0, 0, 0, 0);
    border-width: 0;
    padding: 3px 7px 3px 7px;
    color: var(--mfv-text-value);
}

/* The 70px box holds "100 %" with room to spare. */
.mfv-numberbox .unity-base-text-field__input {
    background-color: rgba(0, 0, 0, 0);
    border-width: 0;
    padding: 3px 7px 3px 7px;
    margin: 0;
    color: var(--mfv-text-value);
    -unity-text-align: middle-right;
}

.mfv-numberbox .unity-text-element--inner-input-field-component {
    padding: 0;
    margin: 0;
}

/* -------------------------------------------------------------- sliders */

/* The whole slider row opts out of clipping so the end thumbs can overhang the rail. */
.mfv-root .mfv-slider {
    overflow: visible;
}

.mfv-root .mfv-slider__input {
    flex-grow: 1;
    flex-direction: row;
    align-items: center;
    /* The thumb is centred on its value, so it hangs 5px past both ends of the rail. */
    overflow: visible;
}

.mfv-slider__track {
    flex-basis: 0;
    flex-grow: 1;
    min-width: 0;
    height: 14px;
    justify-content: center;
    overflow: visible;
}

.mfv-slider__rail {
    position: absolute;
    left: 0;
    right: 0;
    top: 5px;
    height: 4px;
    border-radius: 2px;
    background-color: var(--mfv-track);
}

.mfv-slider__fill {
    position: absolute;
    top: 5px;
    height: 4px;
    border-radius: 2px;
    background-color: var(--mfv-accent-soft);
}

/* Snap marks hang just under the rail, and the thumb hides the one it sits on. */
.mfv-slider__tick {
    position: absolute;
    top: 10px;
    width: 1px;
    height: 4px;
    background-color: var(--mfv-tick);
}

.mfv-slider__thumb {
    position: absolute;
    top: 0;
    width: 11px;
    height: 14px;
    margin-left: -5px;
    background-color: #f0f0f0;
    border-radius: 3px;
}

/*
 * A parameter row is [ slider ][ R ][ S ]. The slider must take the space left after
 * the flags, so its base size is zero and it grows. With flex-basis auto its content
 * width would already fill the row and push the flags outside.
 */
.mfv-root .mfv-animatable__field {
    flex-basis: 0;
    flex-grow: 1;
    min-width: 0;
}

.mfv-animatable__row {
    flex-direction: row;
    align-items: center;
}

/* --------------------------------------------------------------- stepper */

.mfv-root .mfv-stepper__input {
    flex-grow: 0;
    flex-basis: auto;
    flex-direction: row;
    align-items: center;
}

.mfv-stepper__box {
    flex-direction: row;
    align-items: center;
    border-width: 1px;
    border-color: var(--mfv-line-strong);
    border-radius: 4px;
    background-color: var(--mfv-control-bg);
}

.mfv-stepper__button {
    padding: 4px 10px 4px 10px;
    color: var(--mfv-text-sub);
    -unity-text-align: middle-center;
}

/* The value is an MfvNumberBox, a BaseField, so the frame needs .mfv-root to beat the base field margins. */
.mfv-root .mfv-stepper__value {
    margin: 0;
    border-left-width: 1px;
    border-right-width: 1px;
    border-color: var(--mfv-line-strong);
    min-width: 76px;
}

.mfv-root .mfv-stepper__value .unity-base-field__input {
    flex-grow: 1;
}

.mfv-stepper__text {
    flex-grow: 1;
    margin: 0;
    padding: 0;
}

.mfv-stepper__text .unity-base-text-field__input {
    background-color: rgba(0, 0, 0, 0);
    border-width: 0;
    border-radius: 0;
    padding: 4px 12px 4px 12px;
    margin: 0;
    color: var(--mfv-text-value);
    -unity-text-align: middle-center;
}

.mfv-stepper__text .unity-text-element--inner-input-field-component {
    padding: 0;
    margin: 0;
}

/* ---------------------------------------------------------- easing grid */

.mfv-root .mfv-easegrid__input {
    flex-basis: 0;
    flex-grow: 1;
    min-width: 0;
    flex-direction: row;
    flex-wrap: wrap;
}

.mfv-easegrid__tile {
    width: 36px;
    height: 30px;
    background-color: var(--mfv-tile-bg);
    border-width: 1px;
    border-color: var(--mfv-line-tile);
    border-radius: 4px;
    margin: 0 3px 3px 0;
    align-items: center;
    justify-content: center;
}

.mfv-easegrid__tile--selected {
    background-color: var(--mfv-accent-tile);
    border-width: 2px;
    border-color: var(--mfv-accent-soft);
}

.mfv-easegrid__more {
    color: #e2e2e2;
    font-size: 11px;
    -unity-text-align: middle-center;
    padding: 0;
}

.mfv-easegrid__curve {
    width: 24px;
    height: 18px;
}

.mfv-easegrid--compact .mfv-easegrid__tile {
    width: 32px;
    height: 26px;
}

.mfv-easegrid--compact .mfv-easegrid__curve {
    width: 20px;
    height: 15px;
}

/* -------------------------------------------------------- graph preview */

.mfv-graph {
    height: 46px;
    background-color: var(--mfv-well-bg);
    border-width: 1px;
    border-color: var(--mfv-line-tile);
    border-radius: 2px;
}

.mfv-graph--compact {
    height: 32px;
}

/* ------------------------------------------------------------- palettes */

.mfv-palette-strip {
    flex-direction: column;
}

/*
 * The type dropdown (solid | gradient) plus the picker for the selected stop, lined up with
 * the control column by an empty label-width spacer.
 */
.mfv-palette__editor {
    flex-direction: row;
    align-items: center;
    margin-top: 7px;
}

.mfv-root .mfv-palette__editor .unity-base-field__label {
    display: none;
}

.mfv-root .mfv-palette__type {
    width: 116px;
    flex-shrink: 0;
    flex-grow: 0;
    margin-right: var(--mfv-row-gap);
}

.mfv-root .mfv-palette__value {
    flex-basis: 0;
    flex-grow: 1;
    min-width: 0;
}

.mfv-palette {
    flex-direction: row;
    flex-wrap: wrap;
    flex-basis: 0;
    flex-grow: 1;
    min-width: 0;
    align-items: center;
    /* Swatches carry a 4px bottom gutter so wrapped rows don't touch. Give back the last one. */
    margin-bottom: -4px;
}

.mfv-swatch {
    width: 28px;
    height: 28px;
    border-radius: 6px;
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.15);
    margin: 0 4px 4px 0;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.mfv-swatch--selected {
    border-width: 2px;
    border-color: var(--mfv-accent-soft);
}

.mfv-swatch--off {
    background-color: #0c0c0e;
    color: #8a8c94;
    font-size: 8.5px;
    -unity-text-align: middle-center;
    padding: 0;
}

.mfv-swatch--action {
    border-width: 1px;
    border-color: rgba(255, 255, 255, 0.3);
    background-color: rgba(0, 0, 0, 0);
    color: #9a9a9a;
    -unity-text-align: middle-center;
    padding: 0;
}

.mfv-swatch__icon {
    width: 16px;
    height: 16px;
}

/* The swatch being dragged. The strip reorders live underneath it. */
.mfv-swatch--dragging {
    opacity: 0.6;
    border-color: var(--mfv-accent-soft);
}

.mfv-swatch--gobo {
    background-color: #0c0c0e;
}

.mfv-swatch--disabled {
    opacity: 0.4;
}

.mfv-swatch__image {
    width: 100%;
    height: 100%;
}

/* + while the gobo picker is open. `:hover` is listed so the action hover can't hide it. */
.mfv-root .mfv-swatch--active,
.mfv-root .mfv-swatch--active:hover {
    border-color: var(--mfv-accent-soft);
    background-color: var(--mfv-accent-tile);
    color: var(--mfv-text-strong);
}

/* OFF plus the built-in gobos, one click per added stop. */
.mfv-palette__extras {
    flex-direction: column;
}

.mfv-root .mfv-palette__extras > .mfv-row {
    margin-top: 7px;
}

.mfv-root .mfv-gobo-picker {
    /* The box has its own padding, so the strip's -4px give-back would bite into the next row. */
    margin-bottom: 0;
    padding: 6px 2px 2px 6px;
    background-color: var(--mfv-nested-bg);
    border-width: 1px;
    border-color: var(--mfv-line);
    border-radius: 5px;
}

/* ---------------------------------------------------------- effect card */

.mfv-card {
    margin-top: 18px;
}

.mfv-card__header {
    background-color: var(--mfv-header-bg);
    padding: 9px 11px 8px 11px;
    border-width: 1px;
    border-color: var(--mfv-line);
    border-bottom-width: 0;
    border-top-left-radius: 5px;
    border-top-right-radius: 5px;
}

.mfv-card__header--collapsed {
    padding-bottom: 9px;
    border-bottom-width: 1px;
    border-bottom-left-radius: 5px;
    border-bottom-right-radius: 5px;
}

.mfv-card__headerline {
    flex-direction: row;
    align-items: center;
}

.mfv-card__arrow {
    color: #e6e6e6;
    font-size: 11px;
    /* The editor font needs 12px for the glyph, so the gap is trimmed to keep the
       title at 42.5px. */
    width: 12px;
    margin-right: 5px;
    padding: 0;
    -unity-text-align: middle-left;
}

.mfv-card__title {
    -unity-font-style: bold;
    font-size: 14px;
    color: var(--mfv-text-strong);
}

.mfv-card__chips {
    flex-direction: row;
    margin-left: 11px;
}

.mfv-card__chip {
    width: 11px;
    height: 11px;
    border-radius: 2px;
    margin-right: 3px;
}

.mfv-card__actions {
    flex-direction: row;
    align-items: center;
    margin-left: auto;
}

.mfv-card__desc {
    font-size: 12px;
    color: var(--mfv-text-desc);
    padding-left: 19px;
    margin-top: 4px;
    white-space: normal;
}

.mfv-card__body {
    background-color: var(--mfv-panel-bg);
    border-width: 1px;
    border-color: var(--mfv-line);
    border-top-width: 0;
    border-bottom-left-radius: 5px;
    border-bottom-right-radius: 5px;
    /* 12px of padding minus the 9px every child already carries. */
    padding: 11px 11px 3px 11px;
}

.mfv-iconbutton {
    width: 19px;
    height: 19px;
    margin-left: 6px;
    opacity: 0.7;
}

/* ------------------------------------------------------- nested panels */

.mfv-sub {
    background-color: var(--mfv-sub-bg);
    border-radius: 4px;
    border-width: 1px;
    border-color: var(--mfv-line);
    margin: 10px 0 7px 8px;
    padding: 7px 9px 0 9px;
}

/*
 * A frame title set into the top border, like ┌ レンジ ┐. The legend is 16px tall and
 * centred on the 1px border, and the frame pads its top so the first row clears it.
 */
.mfv-root .mfv-sub.mfv-sub--legend {
    padding-top: 14px;
}

.mfv-root .mfv-sub > .mfv-sub__legend {
    position: absolute;
    top: -9px;
    left: 7px;
    height: 16px;
    margin: 0;
}

.mfv-sub__legend-outside {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    height: 9px;
    background-color: var(--mfv-panel-bg);
}

.mfv-sub__legend-inside {
    position: absolute;
    left: 0;
    right: 0;
    top: 9px;
    bottom: 0;
    background-color: var(--mfv-sub-bg);
}

.mfv-root .mfv-sub__title {
    height: 16px;
    padding: 0 4px;
    margin: 0;
    -unity-text-align: middle-left;
    -unity-font-style: bold;
    color: var(--mfv-text-sub);
}

.mfv-sub .unity-base-field__label,
.mfv-sub .mfv-row__label {
    color: var(--mfv-text-sub);
    -unity-font-style: normal;
}

.mfv-nested {
    background-color: var(--mfv-nested-bg);
    border-width: 1px;
    border-color: var(--mfv-line-soft);
    border-radius: 3px;
    padding: 5px 6px 2px 6px;
}

/* ------------------------------------------------------ add effect list */

.mfv-add {
    margin-top: 18px;
}

.mfv-add__button {
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    background-color: var(--mfv-header-bg);
    border-width: 1px;
    border-color: var(--mfv-line);
    border-bottom-width: 0;
    border-top-left-radius: 5px;
    border-top-right-radius: 5px;
    padding: 9px 11px 9px 11px;
}

.mfv-add__button--collapsed {
    border-bottom-width: 1px;
    border-bottom-left-radius: 5px;
    border-bottom-right-radius: 5px;
}

.mfv-add__title {
    -unity-font-style: bold;
    font-size: 14px;
    color: var(--mfv-text-strong);
}

.mfv-add__arrow {
    font-size: 11px;
    color: #e6e6e6;
    padding: 0;
}

.mfv-add__list {
    flex-direction: row;
    flex-wrap: wrap;
    background-color: var(--mfv-panel-bg);
    border-width: 1px;
    border-color: var(--mfv-line);
    border-top-width: 0;
    border-bottom-left-radius: 5px;
    border-bottom-right-radius: 5px;
    /* Three 137.33px tiles + two 8px gutters = 428px, the card's content width.
       Each tile carries its own trailing gutter, so the right padding gives that
       8px back (11 - 8 = 3) and the row still breaks after the third tile. */
    padding: 10px 3px 4px 11px;
}

.mfv-add__item {
    width: 137.33px;
    align-items: center;
    padding: 7px 6px 7px 6px;
    margin: 0 8px 8px 0;
    background-color: var(--mfv-card-bg);
    border-width: 1px;
    border-color: var(--mfv-line-soft);
    border-radius: 5px;
}

.mfv-add__item--last-in-row {
    margin-right: 0;
}

.mfv-add__thumb {
    width: 100%;
    height: 26px;
    background-color: var(--mfv-well-bg);
    border-radius: 3px;
    margin-bottom: 6px;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}

.mfv-add__label {
    font-size: 11px;
    color: #e2e2e2;
    -unity-text-align: middle-center;
}

/* ------------------------------------------------------------- foldout */

.mfv-plainfold {
    background-color: var(--mfv-header-bg);
    border-width: 1px;
    border-color: var(--mfv-line);
    border-radius: 5px;
    padding: 9px 11px 9px 11px;
    margin-top: 18px;
    flex-direction: row;
    align-items: center;
}


.mfv-root .mfv-profile {
    margin-top: 7px;
    padding: 8px 11px 8px 11px;
    background-color: var(--mfv-nested-bg);
    border-width: 1px;
    border-color: var(--mfv-line);
    border-radius: 5px;
}

.mfv-profile__buttons {
    flex-direction: row;
    margin-top: 6px;
}

.mfv-root .mfv-profile__button {
    flex-grow: 1;
    flex-basis: 0;
    margin: 0 4px 0 0;
}

.mfv-root .mfv-profile__button--last {
    margin-right: 0;
}

/* ------------------------------------------------------------------ hover
 *
 * Hover feedback. Every rule that repaints a *selected*
 * control is scoped with .mfv-root so it out-specifies the plain :hover above it.
 * Otherwise hovering a selected segment would grey it out.
 */

.mfv-card__header:hover,
.mfv-plainfold:hover,
.mfv-add__button:hover {
    background-color: #666666;
}

.mfv-iconbutton:hover {
    opacity: 1;
}

.mfv-seg__item:hover {
    background-color: #333333;
    color: var(--mfv-text-strong);
}

.mfv-root .mfv-seg__item--selected:hover {
    background-color: #3d8ae6;
    color: #ffffff;
}

.mfv-tabs__item:hover {
    background-color: #2f2f2f;
    color: var(--mfv-text-strong);
}

.mfv-root .mfv-tabs__item--selected:hover {
    background-color: #3d8ae6;
}

.mfv-switch__track:hover {
    background-color: #5c5c5c;
    border-color: var(--mfv-accent-soft);
}

.mfv-root .mfv-switch__track--on:hover {
    background-color: #3d8ae6;
    border-color: var(--mfv-accent-soft);
}

.mfv-rangeflag__box:hover {
    border-color: var(--mfv-accent-soft);
    color: var(--mfv-text-strong);
}

.mfv-root .mfv-rangeflag__box--on:hover {
    background-color: #3d8ae6;
    border-color: var(--mfv-accent-soft);
}

.mfv-numberbox:hover,
.mfv-textbox:hover {
    border-color: var(--mfv-accent-soft);
}

.mfv-stepper__button:hover {
    background-color: #333333;
    color: #ffffff;
}

.mfv-easegrid__tile:hover {
    background-color: #383838;
    border-color: var(--mfv-accent-soft);
}

.mfv-root .mfv-easegrid__tile--selected:hover {
    background-color: #2a5f96;
    border-color: #9dc7ea;
}

.mfv-swatch:hover {
    border-color: var(--mfv-accent-soft);
}

.mfv-root .mfv-swatch--selected:hover {
    border-color: #9dc7ea;
}

.mfv-swatch--action:hover {
    border-color: var(--mfv-accent-soft);
    color: var(--mfv-text-strong);
}

.mfv-root .mfv-swatch--disabled:hover {
    border-color: rgba(255, 255, 255, 0.3);
    color: #9a9a9a;
}

.mfv-add__item:hover {
    background-color: #3d3d3d;
    border-color: var(--mfv-accent-soft);
}

.mfv-slider__track:hover .mfv-slider__fill {
    background-color: #8fc0ea;
}

.mfv-slider__track:hover .mfv-slider__thumb {
    background-color: #ffffff;
}
